# Navicat 数据生成规则对比

## Navicat 支持的生成规则

根据 Navicat 官方文档和功能，主要支持以下生成规则：

### 1. **随机数值** (Random Number)
- 生成随机整数或浮点数
- 可设置范围和精度
- ✅ **已实现** - `random_number`

### 2. **随机字符串** (Random String)
- 生成随机字符串
- 可指定长度和字符集
- ✅ **已实现** - `random_string`

### 3. **日期和时间** (Date/Time)
- 生成随机日期和时间值
- 可设置范围和格式
- ✅ **已实现** - `random_date`

### 4. **布尔值** (Boolean)
- 生成随机布尔值（true/false）
- ✅ **已实现** - 通过 `list` 规则实现

### 5. **自定义列表** (List)
- 从用户定义的值列表中随机选择
- 支持权重
- ✅ **已实现** - `list`

### 6. **正则表达式** (Regular Expression)
- 根据正则表达式模式生成符合要求的字符串
- ✅ **已实现** - `regex`（基础实现）

### 7. **引用其他字段** (Reference Field)
- 基于同一行中其他字段的值生成数据
- 可以引用同一行的其他字段
- ✅ **已实现** - `reference`

### 8. **序列** (Sequence)
- 生成递增或递减的序列值
- 支持循环
- ✅ **已实现** - `increment`

### 9. **UUID** (UUID)
- 生成唯一标识符（UUID）
- ✅ **已实现** - `function` (UUID)

### 10. **地理数据** (Geographic Data)
- 生成随机的地理位置信息（经纬度）
- 生成地址、城市、国家等
- ✅ **已实现** - `geographic`

### 11. **固定值** (Fixed Value)
- 使用固定值
- ✅ **已实现** - `fixed`

### 12. **从文件读取** (From File)
- 从 CSV/TXT/JSON 文件读取数据
- 支持循环读取
- ✅ **已实现** - `file`

### 13. **模板/表达式** (Template/Expression)
- 使用模板字符串生成复合值
- 支持占位符和表达式
- ✅ **已实现** - `template`（基础实现）

### 14. **空值** (Null)
- 按概率生成 NULL 值
- ✅ **已实现** - `null`

### 15. **外键引用** (Foreign Key)
- 从关联表查询数据
- ✅ **已实现** - 自动处理外键

---

## 对比总结

### ✅ 已实现的规则（13种）

1. ✅ 随机字符串 (`random_string`)
2. ✅ 随机数字 (`random_number`)
3. ✅ 随机日期 (`random_date`)
4. ✅ 固定值 (`fixed`)
5. ✅ 递增序列 (`increment`)
6. ✅ 列表选择 (`list`)
7. ✅ 正则表达式 (`regex`)
8. ✅ 函数 (`function` - UUID, NOW, RAND 等)
9. ✅ 模板 (`template`) - 支持引用其他字段
10. ✅ 空值 (`null`)
11. ✅ 引用其他字段 (`reference`) - **新增**
12. ✅ 地理数据 (`geographic`) - **新增**
13. ✅ 从文件读取 (`file`) - **新增**

---

## Navicat 其他特性

### 1. **表达式支持**
- 支持数学表达式：`{field1} + {field2}`
- 支持字符串函数：`UPPER({name})`, `SUBSTRING({email}, 1, 5)`
- 支持条件表达式：`IF({age} > 18, 'adult', 'minor')`

### 2. **数据分布**
- 支持正态分布、均匀分布等
- 支持自定义分布曲线

### 3. **数据预览**
- 生成前可以预览数据
- 可以调整规则后重新预览

### 4. **批量规则应用**
- 可以批量设置多个字段的规则
- 支持规则模板

### 5. **数据验证**
- 生成前验证规则有效性
- 检查约束冲突

---

## 已实现功能总结

### ✅ 已完成实现

1. ✅ **引用其他字段** (Reference Field)
   - 支持引用同一行中其他字段的值
   - 支持简单的数学表达式（+, -, *, /）
   - 支持字符串拼接
   - 示例：`{first_name}_{last_name}`, `{price} * {quantity}`

2. ✅ **从文件读取** (From File)
   - 支持 CSV、TXT、JSON 文件格式
   - 支持循环读取
   - 文件缓存机制
   - 列索引支持

3. ✅ **地理数据生成** (Geographic Data)
   - 支持城市、国家、地址、坐标、邮编等类型
   - 支持中国和美国的地理数据
   - 可扩展支持更多国家

4. ✅ **增强模板规则**
   - 模板规则现在支持引用其他字段
   - 自动检测字段引用并使用行数据生成

## 建议进一步优化的功能

### 中优先级

1. **表达式支持增强**
   - 当前仅支持简单的二元运算
   - 可以扩展支持更复杂的表达式（如函数调用、条件表达式）
   - 实现难度：较高
   - 使用频率：中
   - 需要表达式解析引擎

### 低优先级

5. **数据分布**
   - 实现难度：较高
   - 使用频率：低
   - 主要用于高级测试场景

6. **数据预览**
   - 实现难度：中等
   - 使用频率：高
   - 提升用户体验

---

## 实现建议

### 引用其他字段功能

**实现方式**：
1. 在生成字段值时，检查模板中是否有 `{field_name}` 占位符
2. 如果存在，从当前行数据中获取对应字段的值
3. 替换占位符

**配置示例**：
```json
{
  "rule_type": "template",
  "config": {
    "template": "{first_name}_{last_name}",
    "reference_fields": ["first_name", "last_name"]
  }
}
```

或新增规则类型：
```json
{
  "rule_type": "reference",
  "config": {
    "field_name": "first_name",
    "expression": "{first_name}_{last_name}"
  }
}
```

### 地理数据生成

**实现方式**：
1. 内置常用城市、国家列表
2. 随机生成经纬度（在合理范围内）
3. 生成地址格式字符串

**配置示例**：
```json
{
  "rule_type": "geographic",
  "config": {
    "type": "city",  // city/country/address/coordinates
    "country": "CN"  // 可选，指定国家
  }
}
```

---

**文档版本**：v1.0  
**最后更新**：2024年

